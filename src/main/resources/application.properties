spring.datasource.url=${env.DB_URL}
spring.datasource.username=${env.DB_USERNAME}
spring.datasource.password=${env.DB_PASSWORD}

spring.security.user.password=${env.SECURITY_USER_PASS}
spring.jpa.properties.hibernate.dialect= org.hibernate.dialect.PostgreSQLDialect
# Hibernate ddl auto (create, create-drop, validate, update)
# For production use validate
spring.jpa.hibernate.ddl-auto= ${env.HIBERNATE_DDL_AUTO}

qr.server.username = ${env.QR_SERVER_USERNAME}
qr.server.password = ${env.QR_SERVER_PASSWORD}

server.port=9000


